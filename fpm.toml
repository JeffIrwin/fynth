
name = "fynth"
version = "0.1.0"
license = "BSD 3-Clause"
author = "Jeff Irwin"

description = "fynth is a synthesizer and audio processor"
categories = ["synthesizer", "audio", "music"]
homepage = "https://github.com/jeffirwin/fynth"

[dependencies]
# TODO: submodule might be better for other build systems (e.g. cmake)
numerical_analysis.git = "https://github.com/jeffirwin/numerical-analysis"
#numerical_analysis.rev = "af5f534"
numerical_analysis.rev = "96ce8e0e135ec36a7efd19fed7f557aeab22fe95"
#numerical_analysis.branch = "main"

[build]
auto-executables = true
auto-tests = true
auto-examples = true
module-naming = true

[install]
library = true
test = false

[fortran]
implicit-typing = false
implicit-external = false
source-form = "free"

